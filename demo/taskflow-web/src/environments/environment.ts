export const environment = {
  production: false,
  apiUrl: '', // Use relative URLs - proxy will handle routing to API
  hubUrl: '/hub/taskflow', // Use relative URL - proxy will handle routing
};
